package Basics;

public class ClassesAndObjectsDemo {

	/*
	 * Object:
	 * anything which we see is an object
	 * 
	 * every object has some properties and some behaviors
	 * 
	 * Properties/Attributes: the way you identify the object, characteristics
	 * Behavior: indicates what is the use of that object
	 * 
	 * Pen as an Object:
	 * Properties: color, cap, size, lenght, weight, material, company, logo, type, price, 
	 * Behavior: writing, drawing, scribbling, 
	 * 
	 * Bike:
	 * Properties: model, company, engine, color, weight, height, mileage, 
	 * Behavior: drive, applyBrake, setGeards, accelerate
	 * 
	 * Human:
	 * Properties: id, age, gender, name, height, weight, color, 
	 * Behavior: walk, talk, think, act, dance, sleeping, eating
	 * 
	 * Classes:
	 * collection of similar objects is class
	 * 
	 * Class: Students
	 * Object: individual student will be the object
	 * 
	 * Teachers
	 * teachers object
	 * 
	 * Properties in java are defined as variables
	 * behavior in java are defined as methods
	 * 
	 */
}
